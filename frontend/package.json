{
  "name": "bustrack-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Static frontend for BusTrack running via a simple local server",
  "scripts": {
    "start": "python -m http.server 5500 --bind 127.0.0.1",
    "serve": "python -m http.server 5500 --bind 127.0.0.1"
  }
}
